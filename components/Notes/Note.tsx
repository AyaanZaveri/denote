import React from 'react'

interface Props {
  title: string
  tag: string
  markdown: string
}

const Note = ({ title, tag, markdown }: Props) => {
  return (
    <div>
      <span></span>
    </div>
  )
}

export default Note
